class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int n = gain.size();
        vector<int> res;
        res.push_back(0);
        for(int i=0;i<n;i++){
            res.push_back(gain[i]);
        }
        vector<int> ans;
        for(int i=0;i<res.size();i++){
            
        }
    }
};
